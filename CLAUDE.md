# AI Disruption Index

This repo is now Codex-oriented. Use `AGENTS.md` as the source of truth for project instructions, search limits, CSV schema, validation rules, and the daily update workflow.

Useful commands:

```bash
uv run python daily_research.py
uv run python validate_csv.py
uv run pytest
```
